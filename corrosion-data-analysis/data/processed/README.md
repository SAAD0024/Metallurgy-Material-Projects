# Generated data

The notebook writes cleaned/processed datasets to this directory when it is run. The raw NIST CSV is intentionally excluded from version control; see the project root README for the official download source.
